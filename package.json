{
  "name": "weekly-project-2",
  "version": "1.0.0",
  "description": "Full-stack app to collect users and store in PostgreSQL",
  "main": "server.js",
  "scripts": {
    "start": "node runMigration.js && node server.js",
    "start:dev": "nodemon server.js",
    "migrate": "node runMigration.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "pg": "^8.10.0"
  },
  "engines": {
    "node": ">=16"
  }
}
